specVersion: "v1beta"
serviceId: "proprol"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "proprol-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
